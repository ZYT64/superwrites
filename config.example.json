# SuperWrites 配置示例

> **真实 API Key 不要写在这里。**
> 启动 TUI 时会引导输入，或用环境变量 `DEEPSEEK_API_KEY`。

```json
{
  "api_key": "ollama",
  "base_url": "http://localhost:11434/v1",
  "model": "qwen",
  "stream": true,
  "self_check_threshold": 0.6,
  "novels_dir": "novels",
  "default_words_per_chapter": 3000,
  "default_target_total_words": 300000,
  "max_retries": 3,
  "directions_count": 3,
  "titles_count": 5
}
```

## 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `api_key` | 是 | API Key。本地 Ollama 填 `ollama` 即可 |
| `base_url` | 是 | API 端点。DeepSeek: `https://api.deepseek.com/v1` |
| `model` | 是 | 模型名。DeepSeek: `deepseek-chat`；Ollama: `qwen`/`llama3` 等 |
| `stream` | 否 | 是否流式输出（推荐 `true`） |
| `self_check_threshold` | 否 | 自检分数阈值，低于此分弹"重生成"窗口（0-1，默认 0.6） |
| `novels_dir` | 否 | 小说项目目录名（默认 `novels`） |
| `default_words_per_chapter` | 否 | 默认每章目标字数（默认 3000） |
| `default_target_total_words` | 否 | 默认整本目标字数（默认 30 万） |
| `max_retries` | 否 | AI 调用失败最大重试次数（默认 3） |
| `directions_count` | 否 | 每次生成的故事方向数（默认 3） |
| `titles_count` | 否 | 每次生成的书名候选数（默认 5） |

## 优先级

1. 环境变量 `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` / `DEEPSEEK_MODEL` 优先
2. `config.json` 其次
3. 默认值兜底
